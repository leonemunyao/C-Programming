#include <stdio.h>

/**
 * main - printing the address of a pointer
 *
 * Return: Always 0
 */

int main(void)
{
int *p;

printf("The address where the pointer has been stored is %p\n", &p);
return (0);
}
