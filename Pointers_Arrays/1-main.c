#include <stdio.h>

/**
 * main - addresses of the variable
 *
 * Return: Always 0
 */

int main(void)
{
char c;
int n;
printf("Address of the variable 'c': %p\n", &c);
printf("Address of the variable 'n': %p\n", &n);
return (0);
}
