#include <stdio.h>

int main(void)
{
float sum = (float) 9 / 2 ;

printf("%f\n", sum);
return (0);
}

// Explicit Type Conversion. Its done manually by placing the type in paranthesis () in front of the line 
// Check the example above
