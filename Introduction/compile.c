#include <stdio.h>

int main() {
    printf("The Compilation process in C follows the following steps: Preprocessor, Compiler, Assembler, and Linker. The preprocessor is the source file that is .c ...The compiler converts the code to assembly language...The assembler converts the assembly code to machine code...The Linker links the code to libraries\n");
    return 0;
}
